var parseEmails = require('./parse_emails.js');

parseEmails.downloadFile()
/*
parseEmails.myFetch()
    .catch(e => {
        console.log('There has been a problem with your fetch operation: ' + e.message)
    .then(parseEmails.parse(e))
    });
*/

//parseEmails.parse();

